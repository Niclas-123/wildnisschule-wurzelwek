<h1><%= @seminar_type.name %></h1>

<% @seminar_instances.each do |s| %>
  <%= link_to s.name, seminar_type_seminar_instance_path(params[:seminar_type_id], s) %>
  <br>
  <%= s.description %>
  <br>
  <%= s.seminar_type.name %>
  <br>
  <hr>
<% end %>
